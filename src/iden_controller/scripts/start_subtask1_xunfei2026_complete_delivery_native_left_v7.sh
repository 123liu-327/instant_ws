#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REFERENCE_FOLLOW_MODE=left exec "$SCRIPT_DIR/start_subtask1_xunfei2026_complete_delivery_native_follow_v7.sh" "$@"
